fn double(x:number) -> number { x * 2 }

fn main() -> number {
    let apply = |f, x| { f(x) };
    let triple = |n| { n * 3 };

    let result1 = apply(double, 5);
    let result2 = apply(triple, 4);

    result1 + result2
}