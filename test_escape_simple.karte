fn test_local() -> number {
    let x = 42;
    let y = x + 10;
    y
}

fn test_param(n: number) -> number {
    n * 2
}

fn main() -> number {
    let a = test_local();
    let b = test_param(20);
    a + b
}
