fn is_even(n: number) -> bool {
    if n == 0 {
        true
    } else {
        is_odd(n - 1)
    }
}

fn is_odd(n: number) -> bool {
    if n == 0 {
        false
    } else {
        is_even(n - 1)
    }
}

fn main() -> number {
    if is_even(2) {
        1
    } else {
        0
    }
}
