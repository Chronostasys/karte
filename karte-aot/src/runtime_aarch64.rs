//! AArch64 AOT 运行时代码生成 (占位, 将来实现)

/// AArch64 运行时 (TODO)
pub struct AArch64Runtime {
    pub code: Vec<u8>,
}

impl AArch64Runtime {
    pub fn new() -> Self {
        Self { code: Vec::new() }
    }

    pub fn generate(self) -> Self {
        // TODO: 实现 AArch64 运行时代码生成
        self
    }
}
