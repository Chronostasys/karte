enum Option {
    Some(number),
    None
}

fn maybe_double(o: Option) -> number {
    match o {
        Some(x) => x * 2,
        None => 0
    }
}

fn main() -> number {
    maybe_double(Some(21)) + maybe_double(None)
}
