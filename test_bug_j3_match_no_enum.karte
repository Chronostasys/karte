fn main() -> number {
    let x = 0;
    for i in 0..2 {
        let r = match i { 0 => 0, _ => 1 };
        x = x + r
    };
    let y = 0;
    while y < 3 {
        x = x + 1;
        y = y + 1
    };
    x + y
}
