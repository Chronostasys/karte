enum List {
    Nil,
    Cons(number, List)
}

fn sum(l: List) -> number {
    match l {
        Nil => 0,
        Cons(h, t) => h + sum(t)
    }
}

fn main() -> number {
    let lst = Cons(1, Cons(2, Cons(3, Nil)));
    sum(lst)
}
