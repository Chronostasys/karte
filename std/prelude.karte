module std.prelude

import std.core::{abs, min, max, clamp}
import std.string::{length, concat, contains, trim_whitespace}
import std.io::{println, print_number, println_number}

fn __prelude_init() -> number {
    0
}
