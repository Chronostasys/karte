module std.prelude

import std.core::{abs, min, max, clamp, sign, is_even, is_odd, floor_div, factorial, gcd, lcm, pow, fibonacci, sum_to, number_to_string}
import std.math::{sqrt_floor, is_prime}
import std.io::{println, print_number, println_number, print_bool, println_bool, debug, debug_string}
import std.string::{length, concat, contains, count_splits, char_code, from_number, is_empty, starts_with, ends_with, repeat}

fn __prelude_init() -> number {
    0
}
