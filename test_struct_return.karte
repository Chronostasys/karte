struct Point {
    x: number,
    y: number
}

fn origin() -> Point {
    Point { x: 0, y: 0 }
}

fn translate(p: Point, dx: number, dy: number) -> Point {
    Point { x: p.x + dx, y: p.y + dy }
}

fn main() -> number {
    let o = origin();
    let p = translate(o, 3, 4);
    p.x + p.y
}
