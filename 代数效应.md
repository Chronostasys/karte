# 代数效应

## 降级实现（伪代码）

### 1 EffectPushHandler

push到栈的内存布局：

```txt
prev_effect_head <- effect_head
tag
handler_label
resume_label (init as 0)
```

这样其实构成一个单向链表，可以用当前的effect_head找到之前handle的位置

```lir
sub sp, sp, #32
store64 [sp + 0], effect_head    ; 保存上一帧
mov effect_head, sp
store64 [sp + 8], <tag or 0>
store64 [sp + 16], handler_label
store64 [sp + 24], 0              ; resume 占位
(if dynamic_tag) store64 [sp + 8], tag_reg
```

### 2 EffectPerform

其实就是循环遍历effect_head，找到对应的handler，然后跳转，如果找不到就跳转到unhandled

```lir
; 分配本地 label：loop, found, unhandled, resume_label
mov walker, effect_head
; 当前effect_head push stack，方便perform之后恢复
sub sp, sp, #8
store64 [sp + 0], effect_head
loop:
  cmp walker, #0
  je unhandled              ; 目标 label 必须已登记；禁止 offset=0 占位写死
  load64 tmp_tag, [walker + 8]
  if dynamic_tag:
      cmp tmp_tag, wanted_tag_reg
  else:
      cmp tmp_tag, #wanted_tag_imm
  je found                  ; 同上，需解析真实偏移
  load64 walker, [walker + 0]
  jmp loop
found:
  store64 [walker + 24], resume_label
  mov r1, payload_reg_or_imm
  load64 target, [walker + 16]
  jmp* target               ; tail jump 到 handler
resume_label:
  ; 恢复effect_head
  load64 effect_head, [sp + 0]
  add sp, sp, #8
  (optional) mov result_reg, r1
  ; 正常继续执行后续代码，不跳转
unhandled:
  ; 模式 A: trap / panic （未来模式 B: 向上传播）
```

### 3 EffectPopHandler

弹出逻辑很简单，就是从栈上弹出effect_head，然后恢复到之前的effect_head

EffectPopHandler 只负责弹出 effect 帧，不包含返回逻辑：

```lir
load64 tmp, [effect_head + 0]
mov effect_head, tmp
add sp, sp, #32
```

由真正的 `Return` 负责统一函数尾声。这样 perform 之后仍可安全执行 pop 之后的代码。

### 4 EffectResume

```lir
mov r1, value
load64 target, [effect_head + 24]
jmp* target
```

## 流程图


