enum Tree {
    Leaf,
    Node(number, Tree, Tree)
}

fn tree_depth(t: Tree) -> number {
    match t {
        Leaf => 0,
        Node(_, l, r) => 1
    }
}

fn main() -> number {
    tree_depth(Node(1, Leaf, Leaf))
}
