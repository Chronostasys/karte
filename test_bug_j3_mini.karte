enum Shape { Cir(number) }
fn main() -> number {
    let x = 0;
    for i in 0..1 {
        let s = Cir(i);
        let r = match s { Cir(r) => r };
        x = x + r
    };
    let y = 0;
    while y < 1 {
        x = x + 1;
        y = y + 1
    };
    x + y
}
