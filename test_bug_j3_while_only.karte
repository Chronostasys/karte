enum Shape { Cir(number) }
fn main() -> number {
    let x = 0;
    let y = 0;
    while y < 3 {
        let s = Cir(y);
        let r = match s { Cir(r) => r };
        x = x + r;
        y = y + 1
    };
    x + y
}
